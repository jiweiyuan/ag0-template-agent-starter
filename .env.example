# Anthropic API Key (required if not using CoreSpeed)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# CoreSpeed AI Gateway (optional, alternative to ANTHROPIC_API_KEY)
# CORESPEED_USER_ID=your-corespeed-user-id
# AI_GATEWAY_URL=https://gateway.ai.c7d.dev

# Server Configuration
PORT=8080
ENV=development

# Frontend dev server port
FRONTEND_PORT=3000

# SQLite database path (optional, defaults to data/chats.db)
# SQLITE_PATH=data/chats.db
